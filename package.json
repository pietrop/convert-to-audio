{
  "name": "@bbc/convert-to-audio",
  "version": "1.1.0",
  "description": "Node ffmpeg wrapper to convert audio or video file to an audio file that meets STT specs",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish:public": "npm publish --access public",
    "publish:dry:run": "npm publish --dry-run"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ffmpeg-static-electron": "^2.0.1",
    "fluent-ffmpeg": "^2.1.2"
  }
}
